language: php

env:
  - MANDRILL_API_KEY=changeme

php:
  - 5.6
  - 7.0
  - 7.1

cache:
  directories:
    - $HOME/.composer/cache

install:
  - composer install --prefer-dist --no-progress --no-suggest --ansi
